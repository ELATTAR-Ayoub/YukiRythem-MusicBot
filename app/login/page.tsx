'use client'

// styles
import styles from '../../styles';
import stylescss from '../../styles/page.module.css';

export default function Page() {

  return (
    <div className={` ${styles.flexCenter}`}>
       <h1>Login</h1>
       <p>We are still choosing a db lol</p>
    </div>
  );
};
